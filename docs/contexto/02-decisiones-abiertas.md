# EPI-Aetheris — Decisiones abiertas

> A diferencia de `01-decisiones-cerradas.md`, nada de esto está resuelto. No invente una respuesta para avanzar una tarea que dependa de un punto de aquí — pregunte al usuario. Cuando algo de esta lista se cierre, muévalo a `01-decisiones-cerradas.md` y bórrelo de aquí; no lo deje duplicado en ambos.

*(Punto A — fórmulas de M3 y M4 — cerrado 2026-09-08, ADR 0019 para M4; M3 ya cerrado 2026-08-21. Movido a `01-decisiones-cerradas.md`; la numeración salta de A a C a propósito, no reordenada.)*

*(Punto B — atribución de fuente climática — cerrado 2026-08-10, ADR 0006. Movido a `01-decisiones-cerradas.md`; la numeración salta de A a C a propósito, no reordenada.)*

*(Punto C — mecanismo de migraciones — cerrado 2026-08-16, ADR 0009. Movido a `01-decisiones-cerradas.md`; la numeración salta de B a D a propósito, no reordenada.)*

## D. Estrategia de pruebas del pipeline de ingesta

No hay `pytest` para el pipeline de boletines (sí existe una prueba para el catálogo de departamentos vs. mapa). El informe compromete un "protocolo de pruebas" y el rol del coordinador dentro del trío de programación es revisión/QA. Insumo ya disponible: tres boletines inspeccionados manualmente con cifras conocidas (`SE232018`, `SE522019_v2`, `SE522023`), usables como casos de referencia sin subir los PDF.

## E. Ubicación de la exclusión de 2020

Definida como ventana de *entrenamiento* (capa de modelado). Si se implementa como filtro en la capa de *ingesta*, se estrecha el estatuto: ingesta debe ser fiel a la fuente y agnóstica al uso posterior. En la práctica solo afecta filas de borde (ej. un "confirmado" de SE52/2020 reportado en el boletín de SE01/2021). No filtrar 2020 durante trabajo de ingesta sin este criterio fijado explícitamente.

## F. Uso de casos recientes como predictor / operación en semanas actuales — sin objeto tras el pivote

Este punto preguntaba cómo comunicar que el clasificador retirado no podía verificarse en vivo. Con la predicción cerrada (`01-decisiones-cerradas.md`, pivote del 2026-08-18), la pregunta ya no aplica en esos términos.

**Cerrado en parte (A1, ADR 0018, 2026-09-08) para la capa climática:** M1/M2 se extienden hasta el año en curso; el aviso de idoneidad y el panel de auditoría declaran que eso es condición biofísica, no incidencia, y que el reanálisis ERA5 tiene ~5 días de rezago. No cierra el refresco recurrente (A2).

La analogía para los módulos descriptivos —la fuente departamental se detiene en 2023— ahora tiene una cifra en M4 (`antiguedad`, semanas desde la última SE con dato, ADR 0019). Queda abierta la pregunta análoga para **M3** (presión epidemiológica relativa): si compara contra casos recientes, depende de esa misma fuente departamental que no existe automatizada después de 2023 — cómo comunicar esa limitación en la interfaz de presión sigue sin definir, y si hace falta un texto de producto específico además de la cifra de M4. Ver punto A.

*(Predicción de casos a corto plazo — **cerrada** 2026-09-09, expuesta en la UI 2026-09-10, ADR 0020. Movida a `01-decisiones-cerradas.md`, "Predicción de casos a corto plazo — nowcast"; el veto a la palabra "predicción" quedó levantado. Lo único que sigue abierto de esa línea está en los pendientes operativos de abajo.)*

*(Punto G — dónde vive la salida de M3/M4 — cerrado 2026-09-08. M3 y M4 se calculan on-request, nada persistido, sin cambio de esquema, igual que M1/M2. M4: `GET /api/v1/vigilancia/integridad`, ADR 0019. Movido a `01-decisiones-cerradas.md`.)*

## H. Semántica acumulada de Probable/Confirmado MINSAL — resuelta operativamente, pero sin objeto de clasificador que desbloquear

**Estado actualizado 2026-08-18.** Este punto documentaba el trabajo de desacumulación del parser MINSAL y el criterio de suficiencia para decidir si un clasificador departamental podía activarse. El trabajo de parser está terminado (ver abajo) y sigue siendo válido — alimenta la capa descriptiva del mapa —, pero **la pregunta que motivaba el punto ("¿se activa el clasificador departamental?") quedó sin objeto**: el pivote a herramienta descriptiva (2026-08-18, `01-decisiones-cerradas.md`) retiró también el clasificador nacional, así que no hay ningún clasificador de ningún nivel al que este reconteo pueda desbloquear. Se conserva el resumen técnico porque el trabajo de extracción/desacumulación sigue siendo la base de la capa descriptiva:

- **Parser de producción: HECHO (2026-08-16).** `backend/ingestion/minsal/parser.py` (ADR 0007, migración `0005`). Corpus completo (264 PDF): 239 `ok`, 25 `ausencia_esperada`, 0 `revision_manual`, 0 `sin_texto_extraible`. 5.805 filas en `casos_epidemiologicos` (fuente `minsal_pdf`, series `probable`+`confirmado`, `boletin_id` trazado). Verificado exacto contra 3 casos de referencia (`SE232018`: 47/21, `SE522019_v2`: 437/174, `SE522023`: 17/54). Detalle del rescate de los 6 boletines problemáticos de 2019 (tarjeta 26) en `03-fuentes-de-datos.md`, trampas 9 y 11.
- **Si en el futuro se retoma cualquier clasificación departamental** (fuera del roadmap actual, que es descriptivo, no clasificador), el reconteo del criterio "pierde >20 % de filas" quedó en 0/14 departamentos para `probable` y 1/14 (San Salvador) para `confirmado` tras el rescate de tarjeta 26 — mejor que el 14/14 previo, pero el piso de suficiencia (12 observaciones / 3 de 4 años base, ventana ±1) seguía sin cumplirse en el 100 % de las celdas departamento-semana. Esa cifra no se ha vuelto a evaluar desde entonces y no debe asumirse vigente sin recalcularla.

## I. Estrategia de implementación de herramientas aprobadas del frontend

El levantamiento de `web/` aprobó técnicamente las herramientas listadas en `01-decisiones-cerradas.md`; quedan abiertos solo los detalles de su implementación:

- **`simple-statistics`:** falta verificar el cumplimiento de su condición departamental/MINSAL: indicador descriptivo definido y contrato de datos disponible. Cuantiles/`ckmeans` no son umbrales epidemiológicos; si existen umbrales MINSAL/OPS, prevalecen. El coroplético no puede comunicar "riesgo" departamental en ningún caso — no hay clasificador de ningún nivel desde el pivote a herramienta descriptiva (2026-08-18); cualquier indicador que `simple-statistics` alimente debe presentarse como descriptivo (M3/M4), nunca como riesgo.
- **Playwright + `@axe-core/playwright`:** falta decidir configuración, scripts, integración CI y el conjunto inicial de smoke tests. Deben usar fixtures o interceptación de API cuando corresponda; los escaneos automáticos no sustituyen las revisiones manuales de accesibilidad.
- **i18n nativo de Astro:** no se activa hasta que exista un requisito de producto para otro idioma. Cuando ocurra, faltará definir rutas, locales, traducciones y calendario de adopción; mientras tanto, evitar dispersar textos nuevos innecesariamente.

Ver alcance, criterios y orden de adopción en `docs/levantamiento-gaps-stack-web.md`.

## J. Cuál copia del frontend es la fuente canónica (abierto 2026-09-13)

Hay dos frontends Astro vivos contra el mismo backend: `web/` en este monorepo y el repo vitrina `aetheris-nitor` (`https://epi-aetheris.dev`), ver "Segundo frontend" en `01-decisiones-cerradas.md`. Al 2026-09-22 divergen en ~57 archivos bajo `src/` y `aetheris-nitor` va adelante (componentes propios, campaña de accesibilidad continuada). Hoy la única regla real es "aplicar el cambio dos veces", que ya produjo trabajo duplicado y copias desincronizadas del mismo componente. **Sin decidir:** si una copia pasa a ser la fuente y la otra se genera o se sincroniza, si se extrae un paquete de componentes compartido, o si se acepta la divergencia y cada sitio evoluciona por su cuenta (y entonces qué se garantiza en cada uno). No mover código entre repos sin que el coordinador fije el criterio.

## K. Alineación del informe con la hipótesis nueva y ejecución de la prueba (abierto 2026-09-23)

La hipótesis y las variables del Capítulo III quedaron cerradas (`01-decisiones-cerradas.md`, "Hipótesis y variables del informe de investigación"). Queda abierto lo que depende de ese cierre, con la Expo Técnica el 29-sep como límite práctico:

- **Resumen Ejecutivo y Capítulo I — redacción propuesta lista (2026-09-23), pendiente de revisión del equipo y de pasarla al Google Docs.** Archivo `Capitulo_I_y_Resumen_EPI-Aetheris.docx`. Cambios: pregunta de investigación reformulada hacia tiempo y exactitud frente a la consulta manual; objetivo general con "desarrollar y evaluar"; objetivo específico nuevo de evaluación de eficiencia (7 objetivos, el último fusiona validación y uso ético); párrafo OMS/El Niño duplicado eliminado; "25 %" corregido a 28,8 % de WIS a h=4 y presentado como resultado técnico complementario; retirada la promesa de "categorías de riesgo alto, medio o bajo" y de "alerta temprana" del impacto; enfoque dual descrito con los módulos M1–M4 reales; "modelado de clasificación" → "modelado estadístico"; alcance del MVP con la evaluación comparativa de eficiencia; limitación nueva sobre el alcance de la prueba (operadores del equipo, no personal de salud). El Resumen Ejecutivo también contradecía el estado actual ("caracteriza el nivel de riesgo de brote", hipótesis climática, 25 %) y se reescribió.
- **Verificar el tablero antes de sortear.** La redacción del 3.3.1 asume que la capa "Volumen de casos (MINSAL)" muestra casos **probables semanales desacumulados** por departamento y semana. Si muestra otra cosa (acumulados, confirmados o solo un agregado anual), hay que ajustar la VI y el valor de referencia antes de ejecutar la prueba, no después. Tener en cuenta que las dos copias del frontend divergen (punto J): fijar en cuál se hace la prueba.
- **Sorteo y valores de referencia.** Falta el script de sorteo con semilla fija (excluyendo semanas sin valor semanal válido y 2020), la lista de las 20 consultas para anexos y el cálculo verificado de los valores de referencia a partir de los boletines.
- **Evaluador externo.** Confirmar disponibilidad y cuáles 5 consultas repite.
- **Prueba estadística opcional.** Con datos pareados se puede aplicar Wilcoxon de rangos con signo si el orientador pide algo más formal que la comparación de promedios. No forma parte del criterio de decisión.
- **Resto del informe.** Los apartados 3.6 en adelante (arquitectura, implementación), la ubicación de los resultados técnicos del nowcast y de la verificación de requerimientos en el Capítulo IV (4.2 y 4.3) y el índice del documento no se reescribieron.

## Pendientes operativos conocidos (no son decisiones, pero sin avance registrado)

- ~~Carga de la serie nacional de OpenDengue a Postgres~~ — **hecho (2026-08-09, tarjeta 11).** `backend/ingestion/cargar_opendengue.py`, `clasificacion='total'` (ADR 0005, migración `0003`); **la carga vigente es 2014–2024, 574 filas semanales** (la cifra original de 365 filas 2018–2024 corresponde a la primera corrida). Ver `01-decisiones-cerradas.md` para el detalle y la verificación contra cifras conocidas.
- ~~Parser de boletines MINSAL de producción (`backend/ingestion/minsal/parser.py`)~~ — **hecho (2026-08-16, tarjeta 22, ADR 0007).** Esta línea quedó sin actualizar cuando el punto H de arriba ya registraba el cierre — corregida aquí para que no contradiga al resto del archivo. Ver punto H para el detalle (239 `ok`, 25 `ausencia_esperada`, 5.805 filas).
- ~~Rescate de los 6 boletines problemáticos de 2019 (3 sospecha tabla-imagen + 3 con mismatch numérico)~~ — **hecho (2026-08-16, tarjeta 26).** Ver punto H arriba y `03-fuentes-de-datos.md`, trampas 9 y 11.
- **Archivar/mover el código del clasificador retirado, pendiente de decidir (abierto 2026-08-20).** Tras el pivote a herramienta descriptiva (2026-08-18, ver `01-decisiones-cerradas.md`), `entrenar_clasificador.py`, `construir_dataset_modelado.py`, `corrida_canal_endemico_nacional.py`, `corrida_canal_endemico_4zonas.py` y los scripts de experimentos descartados (`docs/experimento-*.md` y sus corridas asociadas) siguen viviendo en `backend/ingestion/` junto al código activo de los módulos descriptivos, sin ninguna señal en el árbol de archivos de que son código retirado, no algo a extender. El informe de cierre (`docs/rescate-prediccion/informe-cierre-rescate-prediccion.md`) los recomienda conservar como evidencia reproducible, no borrarlos — pero no dice dónde deben vivir. Sin decidir todavía: si se mueven a una carpeta tipo `backend/ingestion/_retirado/` o `docs/experimentos-cerrados/`, si basta con una nota en el docstring de cada script, o si se dejan donde están confiando en que este documento y `AGENTS.md` ya avisan que no se deben extender. No mover nada sin que el coordinador decida el criterio — mover código real de producción (el que sigue alimentando el canal endémico como comparación histórica descriptiva, si algo de los módulos descriptivos llega a reutilizarlo) sin cuidado podría romper imports. **Avance parcial (no cierra el punto):** la *documentación* del clasificador ya vive aparte desde la reorganización del repo (`docs/clasificador-retirado/`, 2026-09-06, Tech #60) y sus *artefactos* quedaron versionados como excepción explícita del `.gitignore` (`backend/ingestion/data/interim/dataset_modelado/dataset_modelado.csv`, `.../modelo/clasificador_riesgo_nacional_v1.joblib` y `.../modelo/metricas_modelo.json`, issue #72, 2026-09-13) para que las cifras del cierre sean reproducibles por un tercero. **El código sigue donde estaba:** `entrenar_clasificador.py`, `construir_dataset_modelado.py`, `corrida_canal_endemico_nacional.py` y `corrida_canal_endemico_4zonas.py` continúan en `backend/ingestion/` junto al código activo.
- ~~CI~~ — **hecho (2026-08-23).** `.github/workflows/backend-tests.yml` corre `pytest ingestion/tests/ api/tests/` en cada push/PR a `main`, contra un Postgres 15 de servicio con las migraciones de `db/migrations/` montadas en `docker-entrypoint-initdb.d` (mismo mecanismo que `docker-compose.yml`). *Pre-commit* de lint/tests sigue sin implementar. Sí existe `.githooks/pre-commit` (2026-09-08, `core.hooksPath=.githooks`), pero su único trabajo es bloquear los archivos de trabajo local (`*.local.md` y los dos planes legacy por nombre); no corre eslint, prettier ni pytest.
- **Duplicación literal de fórmulas entre `backend/api/idoneidad.py` y `backend/ingestion/validar_leadtime_idoneidad.py` (abierto 2026-08-23; test de regresión añadido 2026-08-29, issue #57).** `idoneidad.py` copia a mano las fórmulas de Iv y el Z-score leave-one-out (mismas constantes, mismo método de resolución de `c`) porque no existe un paquete compartido entre `backend/api` y `backend/ingestion` — el propio docstring de `idoneidad.py` ya avisa "si las formulas cambian en el script de referencia, deben cambiar aqui tambien a mano". El mismo patrón se repite en `backend/api/presion.py` frente a `backend/ingestion/corrida_canal_endemico_nacional.py`. Riesgo: un cambio en el script de `ingestion` (p. ej. tras una nueva validación empírica) puede no propagarse al endpoint que sirve el mapa. **Mitigación actual:** `backend/api/tests/test_formulas_duplicadas.py` importa ambos pares y falla si constantes o resultados sobre insumos sintéticos divergen. Las divergencias deliberadas no se tratan como fallo: alerta binaria / lead time (retirada del pitch), cortes P75/P90 y etiquetas alto/medio/bajo de la corrida nacional, y `PISO_OBSERVACIONES=12` (M3 usa P50/P75, baja/media/alta y piso de años, decisión del coordinador 2026-08-21). **Sigue sin decidir:** si vale la pena un paquete compartido (`backend/common/` o similar). El test no cierra esa decisión; solo convierte el desfase silencioso en un fallo de CI. Extraer el paquete afectaría imports de `backend/api` y `backend/ingestion` a la vez y conviene coordinarlo, no hacerlo de paso en otra tarea.
- **Mapa respiratorio por año + semana (mejora futura, no bloquea `feature/ingesta-respiratoria`).** El mapa actual de IRA/Neumonías representa volumen acumulado 2018–2023 y está rotulado como tal. Explorar un endpoint del tipo `GET /api/neumonias/espacial?anio=2023&semana=27` (y el equivalente de IRA) para pintar una semana concreta queda como tarea independiente. No ampliar esa rama para implementarlo.
- **Regenerar el artefacto del nowcast cuando OpenDengue publique un extracto nuevo (abierto 2026-09-10, ADR 0020).** `backend/api/datos/nowcast_dengue.json` está anclado a 2024-12-22 (última semana de OpenDengue V1.3, ~21 meses detrás del tiempo real). El refresco es manual —recargar la serie, correr `backend/ingestion/nowcast_estimacion_dengue.py`, commitear el JSON— y no hay recordatorio ni chequeo automático de antigüedad del artefacto. Relacionado con A2 (refresco recurrente) del punto F.
- **Probar el mecanismo de fragilidad del nowcast en una segunda serie o ventana (abierto 2026-09-09, ADR 0020).** La robustez ante brotes sin precedente se apoya hoy en **n = 1**: un solo año (2019) con un solo brote grande separa la ventana 2014+ (robusta) de 2016+ (frágil). La segunda confirmación independiente lo señaló como matiz explícito. Hasta que se pruebe en otra serie, la conclusión sobre robustez no debe presentarse como establecida.
- `web/src/styles/tokens.css` y su configuración de Tailwind: convención acordada, no versionada.
- Para el estado real de qué archivos existen en el repo en un momento dado, verificar directamente (`git ls-files`, explorar `backend/`, `docs/adr/`, `db/migrations/`) en vez de confiar en una lista aquí — ese inventario se desactualiza de inmediato (ver el historial en `CHANGELOG.md`: la migración `0002` estuvo documentada como "no existe todavía" en el mismo commit que la creó).
