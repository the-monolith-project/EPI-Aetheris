# Dependencias de desarrollo, no entran a la imagen de producción (el
# Dockerfile del backend solo instala requirements.txt). Instalar con:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==9.0.3
shapely==2.0.4
